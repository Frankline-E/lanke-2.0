# Dockerization

# AI - Prompt

In this lesson, let dockerize our application.
We will dockerize our application for development and production environments.

# Warp prompt commands for dockerization

- https://jsmastery.com/video-kit/310a7f1a-fd45-4a65-bcd5-64b97f810ce6

```bash
You are a senior DevOps engineer. Your task is to dockerize my application that uses Neon Database. The setup must work differently for development and production:

1. **Development Environment (Local):**
    - Use **Neon Local** via Docker.
    - Configure `docker-compose.yml` to run the Neon Local proxy alongside my application. Learn more about Neon Local here: https://neon.com/docs/local/neon-local
    - The application should connect to Postgres at `postgres://user:password@neon-local:5432/dbname` (or equivalent `localhost` inside the compose network).
    - Neon Local should automatically create ephemeral branches for dev and testing.
    - Ensure `.env.development` or equivalent config points to this Neon Local connection string.
2. **Production Environment:**
    - Use the actual **Neon Cloud Database URL** (e.g., `DATABASE_URL=postgres://...neon.tech...`).
    - No Neon Local proxy should be used in production.
    - Ensure secrets and URLs are injected via environment variables, not hardcoded.
    - Create a separate `.env.production` for production environments
3. **General Requirements:**
    - Write a `Dockerfile` for the app.
    - Write a `docker-compose.dev.yml` that runs both the app and Neon Local for development.
    - Write a `docker-compose.prod.yml` that runs both the app and serverless neondb for production.
    - Show how environment variables (`DATABASE_URL`) switch between dev and prod.
    - Provide documentation (in `README.md` style) for how a developer should start the app locally with Neon Local, and how the same app deploys with production Neon DB.
```

# OUT PUT

Listing 1 directory… (ctrl+o to expand)
⎿ $ ls -la /Users/frankline/Projects/Monorepo-Projects/TurboRepo/lanke-2.0/apps/web/app
/Users/frankline/Projects/Monorepo-Projects/TurboRepo/lanke-2.0/apps/web/lib
/Users/frankline/Projects/Monorepo-Projects/TurboRepo/lanke-2.0/apps/user-ui/src 2>&1 | head -100

⏺
(web and user-ui). I'll update tasks and produce the Dockerization files.

⏺ Write(.env.development)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
.env.development
Save file to continue…

Do you want to create .env.development?
⎿ Wrote 15 lines to .env.development
1 # =============================================================================
2 # Development environment
3 # Used by docker-compose.dev.yml + Neon Local
4 # =============================================================================
5
6 # Neon Local proxy (running in Docker). Inside the compose network the host
7 # is `neon-local`. From your host machine use `localhost`.
8 DATABASE_URL=postgres://neon:npg@neon-local:5432/neondb
9
10 # Optional: tell the app it is running in development
… +5 lines (ctrl+o to expand)

⏺ Write(.env.production)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
.env.production
Save file to continue…

Do you want to create .env.production?
⎿ Wrote 16 lines to .env.production
1 # =============================================================================
2 # Production environment
3 # Used by docker-compose.prod.yml
4 # NEVER commit real secrets — supply via your platform's secret manager
5 # (e.g. Docker secrets, AWS SSM, Fly secrets, Vercel env, etc.)
6 # =============================================================================
7
8 # Your real Neon Cloud connection string, e.g.:
9 # postgres://<user>:<password>@<endpoint>.<region>.aws.neon.tech/neondb?sslmode=require
10 DATABASE_URL=postgres://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require
… +6 lines (ctrl+o to expand)

⏺
APP_DIR build arg) and a .dockerignore.

⏺ Write(Dockerfile)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
Dockerfile
Save file to continue…

Do you want to create Dockerfile?
⎿ Wrote 54 lines to Dockerfile
1 # =============================================================================
2 # Multi-stage Dockerfile for the lanke-2.0 Next.js monorepo
3 #
4 # Build a specific app with:
5 # docker build --build-arg APP_DIR=apps/web -t lanke-web .
6 # docker build --build-arg APP_DIR=apps/user-ui -t lanke-user-ui .
7 #
8 # Run:
9 # docker run -p 3001:3001 --env-file .env.production lanke-web
10 # =============================================================================
… +45 lines (ctrl+o to expand)

⏺
enable standalone output.

⏺ Update(apps/web/next.config.ts)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
apps/web/next.config.ts
Save file to continue…

Do you want to make this edit to next.config.ts?
⎿ Added 1 line
2  
 3 const nextConfig: NextConfig = {
4 transpilePackages: ['@workspace/ui'],
5 + output: 'standalone',  
 6 }
7  
 8 export default nextConfig

⏺ Update(apps/user-ui/next.config.ts)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
apps/user-ui/next.config.ts
Save file to continue…

Do you want to make this edit to next.config.ts?
⎿ Added 1 line
2  
 3 const nextConfig: NextConfig = {
4 /_ config options here _/
5 + output: 'standalone',  
 6 }
7  
 8 export default nextConfig

⏺ Write(.dockerignore)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
.dockerignore
Save file to continue…

Do you want to create .dockerignore?
⎿ Wrote 26 lines to .dockerignore
1 node_modules
2 .next
3 .turbo
4 out
5 build
6 dist
7 coverage
8
9 # env
10 .env
… +16 lines (ctrl+o to expand)

⏺ Write(docker-compose.dev.yml)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
docker-compose.dev.yml
Save file to continue…

Do you want to create docker-compose.dev.yml?
⎿ Wrote 83 lines to docker-compose.dev.yml
1 # =============================================================================
2 # Development stack: app(s) + Neon Local proxy
3 # https://neon.com/docs/local/neon-local
4 #
5 # Neon Local creates an ephemeral branch per container — perfect for dev
6 # isolation and integration tests.
7 #
8 # Usage:
9 # docker compose --env-file .env.development -f docker-compose.dev.yml up --build
10 # =============================================================================
… +73 lines (ctrl+o to expand)

⏺ Write(docker-compose.prod.yml)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
docker-compose.prod.yml
Save file to continue…

Do you want to create docker-compose.prod.yml?
⎿ Wrote 59 lines to docker-compose.prod.yml
1 # =============================================================================
2 # Production stack: app(s) only — DATABASE_URL points at Neon Cloud.
3 #
4 # Inject the real connection string via your platform's secret manager.
5 # Example (local sanity-check only):
6 # docker compose --env-file .env.production -f docker-compose.prod.yml up --build
7 #
8 # On a real host (Fly, Railway, Render, ECS, etc.) the secrets come from
9 # the platform — DO NOT pass --env-file in production.
10 # =============================================================================
… +49 lines (ctrl+o to expand)

⏺ Write(DOCKER.md)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
Opened changes in Visual Studio Code ⧉
DOCKER.md
Save file to continue…

Do you want to create DOCKER.md?
⎿ Wrote 157 lines to DOCKER.md
1 # Dockerizing lanke-2.0 (Neon Database)
2
3 This monorepo is Dockerized to run the **same** application code against two
4 different database backends, switched entirely via the `DATABASE_URL` env var:
5
6 | Environment | Database | Source of `DATABASE_URL` |
7 | ----------- | ----------------------- | ------------------------------------ |
8 | Dev | **Neon Local** (Docker) | `.env.development` → `neon-local:5432` |
9 | Prod | **Neon Cloud** | `.env.production` → `*.neon.tech` (injected by the pla
tform in real life) |
… +148 lines (ctrl+o to expand)

⏺
/Users/frankline/Projects/Monorepo-Projects/TurboRepo/lanke-2.0/:

| File | Purpose |
│ File │ Purpose │
├────────────┼─────────┤
│ Dockerfile │ Multi │
└────────────┴─────────┘
└────────────┴────────────────────────────────────────────────────────────────────────────────────────────┘
│ .dockerignore │ │
└───────────────┴─────────────────────────────────────────────────────────────────────────────────────────┘
│ `docker       │                                                                                         │
  └───────────────┴─────────────────────────────────────────────────────────────────────────────────────────┘
  ├─────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
  │ docker-compose.prod.yml │ web + user-ui only — ` │
└─────────────────────────┴───────────────────────────────────────────────────────────────────────────────┘
├─────────────────────────┼───────────────────────────────────────────────────────────────────────────────┤
│ .env.development │ DATABASE_URL=postgres://neon:npg@neon-local:5432/neondb │
└─────────────────────────┴───────────────────────────────────────────────────────────────────────────────┘
│ .env.production │ Template — real Neon Cloud URL with `ssl                                      │
  └─────────────────────────┴───────────────────────────────────────────────────────────────────────────────┘
  │ DOCKER.md               │                                                                               │
  └─────────────────────────┴───────────────────────────────────────────────────────────────────────────────┘
                                      │ Template — real Neon Cloud URL with sslmode=requireone-liners.
──├───────────────────────────────────┼─────────────────────────────────────────────────────────────────────┤──
❯ │ `apps/{ │ Full README: local dev workflow, prod deploy, env switching, │
│ │ one-liners. │
├───────────────────────────────────┼─────────────────────────────────────────────────────────────────────┤
│ apps/{web,user-ui}/next.config.ts │ Added output: 'standalone' so the Dockerfile's runner stage works. │
❯ └───────────────────────────────────┴─────────────────────────────────────────────────────────────────────┘

How the dev/prod switch works

The application code reads only process.env.DATABASE_URL. The two compose files pass different env files into
the same image, so the same code runs against either the local proxy or Neon Cloud with zero changes.

Local dev

docker compose --env-file .env.development -f docker-compose.dev.yml up --build

# web -> http://localhost:3001

# user-ui -> http://localhost:3000

# psql into neon-local:

docker exec -it lanke-neon-local psql -U neon -d neondb

Prod deploy

docker build --build-arg APP_DIR=apps/web -t lanke-web:prod .
docker build --build-arg APP_DIR=apps/user-ui -t lanke-user-ui:prod .

# Set DATABASE_URL via your platform's secret store, expose PORT.

Heads-up

- .env.production is committed as a template only. Real secrets must come from the platform's secret manager
  in prod, not from the file on disk.
- For full Neon Local branching/mirroring into your real project, supply NEON_API_KEY + NEON_PROJECT_ID as
  env vars when starting the dev compose (documented in DOCKER.md and stubbed in docker-compose.dev.yml).
- .gitignore already excludes .env\*, so the new env files would be untracked locally — I've kept them in
  place for you to copy as templates; add !.env.development / !.env.production to .gitignore if you want them
  committed.

✻ Cooked for 2m 53s

※ recap: You asked me to dockerize the lanke-2.0 Next.js monorepo with Neon Database, switching between Neon
Local for dev and Neon Cloud for prod via DATABASE_URL. All Dockerfiles, compose files, env templates, and a
DOCKER.md readme were just created. Next: review DOCKER.md and run `docker compose --env-file 
  .env.development -f docker-compose.dev.yml up --build` to confirm. (disable recaps in /config)

───────────────────────────────────────────────────────────────────────────────────────────────────────────────
❯  
───────────────────────────────────────────────────────────────────────────────────────────────────────────────
⏸ manual mode on · ? for shortcuts · ← for agents ⧉ In 04. Dockerization.md

# Docker File

    ```Dockerfile
    # =============================================================================
    # Multi-stage Dockerfile for the lanke-2.0 Next.js monorepo
    #
    # Build a specific app with:
    #   docker build --build-arg APP_DIR=apps/web      -t lanke-web      .
    #   docker build --build-arg APP_DIR=apps/user-ui  -t lanke-user-ui  .
    #
    # Run:
    #   docker run -p 3001:3001 --env-file .env.production lanke-web
    # =============================================================================

    # ---------- 1. Dependencies -----------------------------------------------
    FROM node:20-alpine AS deps
    RUN corepack enable && corepack prepare pnpm@11.22.0 --activate
    WORKDIR /repo

    # Copy the manifests Turborepo/pnpm need to resolve the workspace.
    COPY pnpm-workspace.yaml package.json pnpm-lock.yaml* .npmrc ./
    COPY apps ./apps
    COPY packages ./packages

    RUN pnpm install --frozen-lockfile

    # ---------- 2. Build ------------------------------------------------------
    FROM deps AS builder
    ARG APP_DIR
    ENV NEXT_TELEMETRY_DISABLED=1

    WORKDIR /repo/apps/$(basename ${APP_DIR})
    RUN pnpm build

    # ---------- 3. Runner -----------------------------------------------------
    FROM node:20-alpine AS runner
    ARG APP_DIR
    ENV NODE_ENV=production
    ENV NEXT_TELEMETRY_DISABLED=1
    ENV PORT=3000
    ENV HOSTNAME=0.0.0.0

    WORKDIR /app

    # Non-root user
    RUN addgroup --system --gid 1001 nodejs \
    && adduser  --system --uid 1001 nextjs

    # Copy the standalone server output from Next.js
    COPY --from=builder --chown=nextjs:nodejs /repo/${APP_DIR}/.next/standalone ./
    COPY --from=builder --chown=nextjs:nodejs /repo/${APP_DIR}/.next/static     ./${APP_DIR}/.next/static
    COPY --from=builder --chown=nextjs:nodejs /repo/${APP_DIR}/public           ./${APP_DIR}/public

    USER nextjs
    EXPOSE 3000

    CMD ["node", "server.js"]

    ```

# Docker Compose For Development

- docker-compose.dev.yml

  ```yaml
  # =============================================================================
  # Development stack: app(s) + Neon Local proxy
  # https://neon.com/docs/local/neon-local
  #
  # Neon Local creates an ephemeral branch per container — perfect for dev
  # isolation and integration tests.
  #
  # Usage:
  #   docker compose --env-file .env.development -f docker-compose.dev.yml up --build
  # =============================================================================

  services:
    # ---------- Neon Local proxy ------------------------------------------
    # Acts as a regular Postgres server on :5432 but is backed by an ephemeral
    # Neon branch in your real Neon project.
    neon-local:
      image: neondatabase/neon_local:latest
      container_name: lanke-neon-local
      ports:
        - '5432:5432'
      environment:
        PARENT_PROJECT_ID: ${NEON_PARENT_PROJECT_ID:-}
        PARENT_BRANCH_ID: ${NEON_PARENT_BRANCH_ID:-main}
        # Creates a fresh branch on every container start.
        CREATE_BRANCH: ${NEON_CREATE_BRANCH:-true}
        # Auto-delete the branch when the container stops.
        DELETE_BRANCH: ${NEON_DELETE_BRANCH:-true}
        # Mirror writes back to the parent branch.
        BRANCH_MIRROR: ${NEON_BRANCH_MIRROR:-false}
        DRIVER: ${NEON_DRIVER:-postgres}
        # PG password the app uses to connect (matches .env.development).
        PGUSER: neon
        PGPASSWORD: npg
        PGDATABASE: neondb
      healthcheck:
        test: ['CMD-SHELL', 'pg_isready -U neon -d neondb']
        interval: 5s
        timeout: 3s
        retries: 20

    # ---------- web app ----------------------------------------------------
    web:
      build:
        context: .
        dockerfile: Dockerfile
        args:
          APP_DIR: apps/web
      container_name: lanke-web-dev
      image: lanke-web:dev
      depends_on:
        neon-local:
          condition: service_healthy
      environment:
        DATABASE_URL: ${DATABASE_URL}
        NODE_ENV: development
        PORT: ${WEB_PORT:-3001}
        HOSTNAME: 0.0.0.0
      ports:
        - '${WEB_PORT:-3001}:${WEB_PORT:-3001}'

    # ---------- user-ui app ------------------------------------------------
    user-ui:
      build:
        context: .
        dockerfile: Dockerfile
        args:
          APP_DIR: apps/user-ui
      container_name: lanke-user-ui-dev
      image: lanke-user-ui:dev
      depends_on:
        neon-local:
          condition: service_healthy
      environment:
        DATABASE_URL: ${DATABASE_URL}
        NODE_ENV: development
        PORT: ${USER_UI_PORT:-3000}
        HOSTNAME: 0.0.0.0
      ports:
        - '${USER_UI_PORT:-3000}:${USER_UI_PORT:-3000}'

  networks:
    default:
      name: lanke-dev
  ```

# Docker Compose File For Production

- docker-compose.prod.yml

  ```yaml
  # =============================================================================
  # Production stack: app(s) only — DATABASE_URL points at Neon Cloud.
  #
  # Inject the real connection string via your platform's secret manager.
  # Example (local sanity-check only):
  #   docker compose --env-file .env.production -f docker-compose.prod.yml up --build
  #
  # On a real host (Fly, Railway, Render, ECS, etc.) the secrets come from
  # the platform — DO NOT pass --env-file in production.
  # =============================================================================

  services:
    web:
      build:
        context: .
        dockerfile: Dockerfile
        args:
          APP_DIR: apps/web
      image: lanke-web:prod
      container_name: lanke-web-prod
      restart: unless-stopped
      environment:
        DATABASE_URL: ${DATABASE_URL}
        NODE_ENV: production
        PORT: ${WEB_PORT:-3001}
        HOSTNAME: 0.0.0.0
      ports:
        - '${WEB_PORT:-3001}:${WEB_PORT:-3001}'
      healthcheck:
        test: ['CMD', 'wget', '-qO-', 'http://127.0.0.1:${WEB_PORT:-3001}/']
        interval: 30s
        timeout: 5s
        retries: 3

    user-ui:
      build:
        context: .
        dockerfile: Dockerfile
        args:
          APP_DIR: apps/user-ui
      image: lanke-user-ui:prod
      container_name: lanke-user-ui-prod
      restart: unless-stopped
      environment:
        DATABASE_URL: ${DATABASE_URL}
        NODE_ENV: production
        PORT: ${USER_UI_PORT:-3000}
        HOSTNAME: 0.0.0.0
      ports:
        - '${USER_UI_PORT:-3000}:${USER_UI_PORT:-3000}'
      healthcheck:
        test: ['CMD', 'wget', '-qO-', 'http://127.0.0.1:${USER_UI_PORT:-3000}/']
        interval: 30s
        timeout: 5s
        retries: 3

  networks:
    default:
      name: lanke-prod
  ```

# Summary

# DOCKER.md

```file
# Dockerizing lanke-2.0 (Neon Database)

This monorepo is Dockerized to run the **same** application code against two
different database backends, switched entirely via the `DATABASE_URL` env var:

| Environment | Database                | Source of `DATABASE_URL`             |
| ----------- | ----------------------- | ------------------------------------ |
| Dev         | **Neon Local** (Docker) | `.env.development` → `neon-local:5432` |
| Prod        | **Neon Cloud**          | `.env.production` → `*.neon.tech` (injected by the platform in real life) |

> Reference: <https://neon.com/docs/local/neon-local>

---

## Files added

```

.
├── Dockerfile # multi-stage, Next.js standalone output
├── .dockerignore
├── docker-compose.dev.yml # app + neon-local
├── docker-compose.prod.yml # app only — Neon Cloud
├── .env.development # commits OK (no secrets)
└── .env.production # template only — never commit real secrets

````

The Dockerfile builds whichever app you pass via `--build-arg APP_DIR=apps/...`.

---

## 1. Development — Neon Local

Neon Local is a Docker image (`neondatabase/neon_local`) that proxies Postgres
on `:5432` but is backed by a real **ephemeral branch** in your Neon project.
On every container start it creates a fresh branch; on stop it deletes it
(set `NEON_DELETE_BRANCH=false` to keep it for debugging).

### Prereqs

* Docker + Docker Compose v2
* A Neon project + API key (only required if you want the ephemeral branch
  to be a *real* Neon branch — `CREATE_BRANCH=false` runs in fully-local mode).

### Start the dev stack

```bash
# 1. (optional) link Neon Local to a real Neon project for branch mirroring
export NEON_API_KEY=<your-neon-api-key>
export NEON_PROJECT_ID=<your-neon-project-id>
export NEON_PARENT_BRANCH_ID=main

# 2. Boot Neon Local + both Next.js apps
docker compose --env-file .env.development -f docker-compose.dev.yml up --build
````

You should see:

| Service      | URL                                         |
| ------------ | ------------------------------------------- |
| `web`        | <http://localhost:3001>                     |
| `user-ui`    | <http://localhost:3000>                     |
| `neon-local` | `postgres://neon:npg@localhost:5432/neondb` |

Inside the Docker network the app talks to `neon-local:5432`. From your host
use `localhost:5432`. Same auth, same DB — both resolve to the ephemeral
branch.

### Tearing down / resetting

```bash
# Stop containers (deletes the ephemeral branch)
docker compose -f docker-compose.dev.yml down

# Wipe volumes too
docker compose -f docker-compose.dev.yml down -v
```

---

## 2. Production — Neon Cloud

In production there is **no Neon Local proxy**. The app talks straight to
Neon's serverless Postgres over TLS.

### Build the images

```bash
docker build --build-arg APP_DIR=apps/web     -t lanke-web:prod     .
docker build --build-arg APP_DIR=apps/user-ui -t lanke-user-ui:prod .
```

### Run with Docker Compose (local sanity-check only)

```bash
# Put the real Neon URL into .env.production first, then:
docker compose --env-file .env.production -f docker-compose.prod.yml up -d
```

> ⚠️ Don't actually deploy this way with the URL on disk. In production the
> `DATABASE_URL` must come from your platform's secret store.

### Run on a host (Fly.io / Railway / Render / ECS / K8s / etc.)

The image only needs:

```
DATABASE_URL=postgres://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
NODE_ENV=production
PORT=3001
HOSTNAME=0.0.0.0
```

Set those four and expose `PORT`. Done.

---

## 3. How the env switch works

`DATABASE_URL` is the _only_ thing that changes:

- `docker-compose.dev.yml` injects `.env.development`, whose `DATABASE_URL`
  points at the `neon-local` service.
- `docker-compose.prod.yml` injects `.env.production`, whose `DATABASE_URL`
  is your real Neon Cloud URL.

Both compose files build the same image. The application code reads
`process.env.DATABASE_URL` and never knows whether it's talking to a
local proxy or Neon Cloud — that's the whole point of Neon Local.

### `.env.development`

```env
DATABASE_URL=postgres://neon:npg@neon-local:5432/neondb
NODE_ENV=development
```

### `.env.production` (template — fill in or override via secrets)

```env
DATABASE_URL=postgres://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require
NODE_ENV=production
```

---

## 4. Useful one-liners

```bash
# Open a psql shell into Neon Local from your host
docker exec -it lanke-neon-local psql -U neon -d neondb

# Tail only the app logs
docker compose -f docker-compose.dev.yml logs -f web user-ui

# Rebuild after a code change
docker compose -f docker-compose.dev.yml up --build
```

```

```
